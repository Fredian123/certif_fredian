# Plan d'implémentation : Correction et Optimisation de datatool

**But** : Diagnostiquer et corriger les anomalies de l'utilitaire `datatool`, optimiser le regroupement algorithmique et ajouter l'export JSON ainsi qu'une couverture de tests complète.
**Architecture** : Approche modulaire séparant les fonctions pures de calcul et traitement (`traitement.py`) du point d'entrée et de la gestion des E/S (`datatool.py`). Optimisation du regroupement par dictionnaire pour atteindre une complexité linéaire $O(N)$ au lieu de quadratique $O(N^2)$.
**Tech Stack** : Python 3.x, Unittest standard library.

---

### Tâche 1 : Correction du filtrage par seuil (inclusif >=)
**Fichiers** : 
- `c:\rattrapage_L3_Examenfinale\Depot_degrade_Etudiant_2\datatool\tests\test_traitement.py`
- `c:\rattrapage_L3_Examenfinale\Depot_degrade_Etudiant_2\datatool\traitement.py`

- [ ] **Étape 1 : Écrire le test qui échoue** (dans `test_traitement.py`) :
  ```python
  def test_filtrer_par_seuil_inclusif(self):
      # Le seuil de 4 doit inclure la valeur 4 (supérieur ou égal)
      res = traitement.filtrer_par_seuil([1, 4, 10], 4)
      self.assertEqual(res, [4, 10])
  ```
- [ ] **Étape 2 : Vérifier l'échec du test** :
  ```bash
  python -m unittest discover tests
  ```
- [ ] **Étape 3 : Écrire l'implémentation minimale** (dans `traitement.py`) :
  ```python
  def filtrer_par_seuil(valeurs, seuil):
      """Filtre une liste de valeurs numeriques selon un seuil (superieur ou egal)."""
      resultat = []
      for v in valeurs:
          if v >= seuil:
              resultat.append(v)
      return resultat
  ```
- [ ] **Étape 4 : Vérifier le succès du test** :
  ```bash
  python -m unittest discover tests
  ```
- [ ] **Étape 5 : Committer** :
  ```bash
  git add tests/test_traitement.py traitement.py
  git commit -m "Corrige filtrer_par_seuil pour inclure la borne supérieure ou égale"
  ```

---

### Tâche 2 : Correction et optimisation du regroupement sans doublons et trié
**Fichiers** :
- `c:\rattrapage_L3_Examenfinale\Depot_degrade_Etudiant_2\datatool\tests\test_traitement.py`
- `c:\rattrapage_L3_Examenfinale\Depot_degrade_Etudiant_2\datatool\traitement.py`

- [ ] **Étape 1 : Écrire les tests qui échouent** (dans `test_traitement.py`) :
  ```python
  def test_regrouper_sans_doublons(self):
      donnees = [
          {"site": "Nord", "capteur": "C1", "valeur": 12.5},
          {"site": "Nord", "capteur": "C2", "valeur": 8.0},
      ]
      groupes = traitement.regrouper(donnees, "site")
      self.assertEqual(len(groupes), 1)
      self.assertEqual(len(groupes[0]["membres"]), 2)

  def test_regrouper_tri_croissant(self):
      donnees = [
          {"site": "Sud", "capteur": "C1", "valeur": 15.2},
          {"site": "Nord", "capteur": "C1", "valeur": 12.5},
      ]
      groupes = traitement.regrouper(donnees, "site")
      self.assertEqual(groupes[0]["cle"], "Nord")
      self.assertEqual(groupes[1]["cle"], "Sud")
  ```
- [ ] **Étape 2 : Vérifier l'échec des tests** :
  ```bash
  python -m unittest discover tests
  ```
- [ ] **Étape 3 : Écrire l'implémentation minimale optimisée** (dans `traitement.py`) :
  ```python
  def regrouper(enregistrements, cle):
      """Regroupe une liste de dictionnaires selon la valeur d'une cle et trie par cle croissante."""
      groupes_dict = {}
      for e in enregistrements:
          k = e[cle]
          if k not in groupes_dict:
              groupes_dict[k] = []
          groupes_dict[k].append(e)
      
      groupes = []
      for k in sorted(groupes_dict.keys()):
          groupes.append({"cle": k, "membres": groupes_dict[k]})
      return groupes
  ```
- [ ] **Étape 4 : Vérifier le succès des tests** :
  ```bash
  python -m unittest discover tests
  ```
- [ ] **Étape 5 : Committer** :
  ```bash
  git add tests/test_traitement.py traitement.py
  git commit -m "Optimise la fonction regrouper en O(N log K) sans doublons et triée par clé"
  ```

---

### Tâche 3 : Implémentation de l'export JSON et amélioration de l'analyse d'arguments
**Fichiers** :
- `c:\rattrapage_L3_Examenfinale\Depot_degrade_Etudiant_2\datatool\datatool.py`

- [ ] **Étape 1 : Mettre à jour `datatool.py`** :
  - Ajouter l'import `json`.
  - Prendre en charge `--json <fichier.json>`.
  - Exporter les groupes au format JSON si l'option est présente.
- [ ] **Étape 2 : Valider manuellement** :
  ```bash
  python datatool.py mesures_exemple.csv --json sortie.json
  ```
- [ ] **Étape 3 : Valider le contenu du fichier JSON généré** :
  ```bash
  cat sortie.json
  ```
- [ ] **Étape 4 : Committer** :
  ```bash
  git add datatool.py
  git commit -m "Implémente l'export JSON et le parsing des arguments optionnels"
  ```

---

### Tâche 4 : Documentation de maintenance
**Fichiers** :
- `c:\rattrapage_L3_Examenfinale\Depot_degrade_Etudiant_2\datatool\README.md`

- [ ] **Étape 1 : Rédiger la documentation de maintenance** :
  - Mettre à jour `README.md` avec l'explication de l'architecture, la complexité des algorithmes et les parties critiques.
- [ ] **Étape 2 : Committer** :
  ```bash
  git add README.md
  git commit -m "Met à jour la documentation README.md avec l'architecture et les informations de maintenance"
  ```
